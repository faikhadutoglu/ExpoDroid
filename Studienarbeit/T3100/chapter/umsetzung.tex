\chapter{Umsetzung und Ergebnisse}
\label{cha:Umsetzung und Ergebnisse}

\section{Mechanische Konstruktion}
\label{cha:Mechanische Konstruktion}

\subsection{CAD-Modellierung}
\label{cha:CAD-Modellierung}

Vor der praktischen Umsetzung des Roboters wurde zunächst ein digitales CAD-Modell erstellt. Ziel dieser Modellierung war es, bereits in einer frühen Projektphase einen groben, jedoch strukturierten Bauplan zu entwickeln, der sowohl die äußere Gestaltung als auch den grundsätzlichen mechanischen Aufbau des Systems abbildet. Dadurch konnten gestalterische, konstruktive und funktionale Aspekte frühzeitig bewertet und iterativ angepasst werden.

Die CAD-Modellierung erfolgte mit einer browserbasierten, cloudgestützten CAD-Anwendung (Onshape\reg). Ein wesentlicher Vorteil dieses Werkzeugs liegt in der Möglichkeit zur kollaborativen Zusammenarbeit, da mehrere Projektmitglieder zeitgleich an einem Modell arbeiten können. Dies erleichtert die Arbeitsteilung innerhalb eines studentischen Projekts erheblich und ermöglicht eine kontinuierliche Weiterentwicklung des Entwurfs unabhängig vom verwendeten Endgerät oder Standort.

Der CAD-Entwurf diente primär als konzeptionelle Grundlage für den späteren physischen Aufbau des Roboters. Dabei lag der Fokus weniger auf einer vollständig fertigungsgerechten Detailkonstruktion, sondern vielmehr auf der Definition der grundlegenden Struktur, der Proportionen sowie der Anordnung zentraler Komponenten. Auf diese Weise konnten sowohl stilistische Aspekte des Designs als auch technische Randbedingungen untersucht und aufeinander abgestimmt werden.

Ein weiterer wichtiger Zweck der CAD-Modellierung bestand in der Abschätzung von Materialbedarf, Bauraum und Bauteilpositionierungen. Insbesondere die Dimensionierung der tragenden Struktur sowie die Platzierung von Antriebskomponenten, Elektronik und Verkleidungselementen konnten im digitalen Modell überprüft und optimiert werden. Darüber hinaus ließ sich bereits vor dem Bau einschätzen, welche Fertigungsschritte erforderlich sind und welche Bauteile mit den zur Verfügung stehenden Mitteln realisierbar sind.

Die Verwendung eines digitalen Modells vor Beginn der mechanischen Fertigung trägt wesentlich zur Vermeidung von Konstruktionsfehlern bei. Größere Änderungen lassen sich im CAD-Modell mit vergleichsweise geringem Aufwand umsetzen, während nachträgliche Anpassungen am physischen Aufbau häufig mit erheblichem Zeit- und Kostenaufwand verbunden sind. Durch die vorgelagerte Modellierung konnten somit sowohl der Materialverbrauch als auch das Risiko eines vollständigen Neubaus reduziert werden.

\subsection{Montage und Nachbearbeitung}
\label{cha:Montage und Nachbearbeitung}

Auf Basis des CAD-Modells wurde schließlich die Entscheidung für den Werkstoff Holz als primäres Konstruktionsmaterial getroffen. Holz bietet im Vergleich zu Metall ein geringeres Gewicht bei gleichzeitig ausreichender Stabilität für den geplanten Einsatzzweck. Zudem lässt es sich mit einfachen Werkzeugen bearbeiten und eignet sich daher besonders für die eigenständige Fertigung im Rahmen eines studentischen Projekts. Im Vergleich zu Kunststoffbauteilen aus dem 3D-Druck war das verfügbare Druckvolumen nicht ausreichend, um größere tragende Strukturelemente am Stück herzustellen.

Der aktuelle Stand der mechanischen Konstruktion basiert auf einer groben Holzstruktur, die die grundlegende Form und Tragfähigkeit des Roboters vorgibt. Diese Struktur dient als funktionales Grundgerüst und soll in einem weiteren Schritt durch zusätzliche Verkleidungselemente ergänzt werden. Geplant ist, diese Verkleidung teilweise mittels additiver Fertigung herzustellen und auf die Holzstruktur aufzuschrauben, um die Optik zu verbessern.

\begin{figure}[htb]
	\centering
	\fbox{%
		%		\includegraphics[width=0.5\linewidth]{images/Sphärischer_Roboter.jpg}
		\includegraphics[width=0.5\linewidth]{images/CAD_Roboter.png}
	}
	\caption{CAD-Modell des Roboters (ohne Arme)}
	\label{fig:CAD_Roboter}
\end{figure}

Der würfelförmige Plattformkörper besitzt eine Abmessungen von
\SI{450}{\milli\metre} mal \SI{450}{\milli\metre}.
Mit Rädern und Kopfelement erreicht der Roboter nach aktueller Auslegung eine Gesamthöhe von \SI{925}{\milli\metre}.




%\subsection{3D-Druck der Komponenten \textcolor{red}{bis jetzt nicht genutzt nur option für Zukunft}}

%\subsection{Motoransteuerung und Treiber-Konfiguration \textcolor{red}{Faik \& Hai Nam, keine ahnung was davon schon in folgenden Kapiteln aufgegriffen oder erklährt wurde ///Faiks comment:ich würd dieesen Abschnitt weglassen}}
%Der Roboter wird mit der Fernbedienung \textcolor{blue}{[Modell Einsetzten]} über WLAN angesteuert\textcolor{red}{[ist diese info nicht falsch ist das über Wlan?]} . Die Bewegung des Joysticks wirkt sich wie folgend in Abbildung \ref{fig:Joystick_Controler} gezeigt aus. Bewegung des Sticks nach Norden bedeutet vorwärtsfahren und nach Süden Rückwärtsfahren. Die weite in die jeweilige Richtung bestimmt dabei die an den Motoren anliegende Spannung, damit die Geschwindigkeit. In Ruhelage, sprich in der Mitte stoppt der Roboter. Wird der Joystick nicht betätigt, geht dieser von alleine zurück in die Ruheposition, damit lässt sich der Roboter nur mit aktiver Eingabe des Benutzers in Bewegung versetzten.
%
%
%
%\begin{figure}[hbt]
%	\centering
%	\input{images/Joystick_Controler.tex}
%	\caption{Zeigt die Stelllagen des Steuerungs-Joysticks für Fahren und Lenken des Roboters}		
%	\label{fig:Joystick_Controler}
%\end{figure}

%\subsection{Energieversorgungssystem}
%\subsection{Sicherheitsschaltungen}
%

\section{Motorsteuerung und Bewegungsalgorithmen}
\label{cha:Motorsteuerung und Bewegungsalgorithmen}

Nachdem in der Konzeptionsphase (siehe Kapitel \ref{sec:Steuerungskonzept}) das Prinzip des Differentialantriebs festgelegt wurde, beschreibt dieser Abschnitt die konkrete softwareseitige Umsetzung auf dem Mikrocontroller. Ziel der Implementierung war es, die Steuersignale einer Standard-Modellbaufernsteuerung so zu verarbeiten, dass damit leistungsstarke Gleichstrommotoren über BTS7960-H-Brücken präzise geregelt werden können.

\subsection{Fernsteuerungssystem und Signalübertragung}
\label{cha:Fernsteuerungssystem und Signalübertragung}

Der Sender \textbf{Spektrum NX8} wird als HMI für die manuelle Fernsteuerung des Roboters verwendet. Dieses Modell ermöglicht eine präzise Übermittlung der Steuerbefehle in Echtzeit. Die Kommunikation zwischen Sender und Roboter erfolgt drahtlos im \SI{2.4}{\giga\hertz}-Frequenzband unter Verwendung des DSMX-Protokolls. \autocite{NX8}

Hardwareseitig besteht das System aus zwei Hauptkomponenten:
\begin{enumerate}
	\item \textbf{Sender (Spektrum NX8):} Erfasst die mechanischen Auslenkungen der Steuerknüppel und wandelt diese in digitale Datenpakete um.
	\item \textbf{Empfänger:} Dieser ist fest auf dem Roboter verbaut und elektrisch direkt mit den GPIO-Pins des Mikrocontrollers verbunden. Er demoduliert das Hochfrequenzsignal und gibt es als PWM-Signale an die Steuereinheit weiter.
\end{enumerate}

Das Steuerungsschema wurde intuitiv ausgelegt\footnote{Zusätzlich existiert ein weiterer Kanal zur Steuerung des Kopfes. Da der Kopf jedoch nicht in den Körper integriert wurde, wird darauf nicht näher eingegangen.}:
\begin{itemize}
	\item \textbf{Linker Joystick (Y-Achse):} Steuert die translatorische Bewegung. Ein Auslenken nach oben bewirkt eine Vorwärtsfahrt, nach unten eine Rückwärtsfahrt(siehe Abbildung \ref{fig:joystick_pwm}).
	\item \textbf{Rechter Joystick (X-Achse):} Steuert die rotatorische Bewegung. Eine Auslenkung nach rechts oder links induziert die entsprechende Lenkbewegung durch Differenzialsteuerung der Räder.
\end{itemize}


\subsection{Programmablauf und Architektur}
\label{cha:Programmablauf und Architektur}

Die Steuerung basiert auf einem zyklischen Algorithmus, der kontinuierlich die Eingabesignale überwacht und in Motorbewegungen umsetzt. Dieser Prozess ist in Abbildung \ref{fig:flowchart_motor} als Programmablaufplan (englisch: Flowchart) dargestellt. Er verdeutlicht den Weg vom rohen Sensorsignal bis zur physikalischen Ansteuerung der Motortreiber.


\begin{figure}[hbt]
	\centering
	\includegraphics[width=0.9\linewidth]{images/Motorflowchart.png}
	\vspace{0.1cm}
	\caption{Programmablaufplan der implementierten Motorsteuerung: Vom RC-Signal zur Motorbewegung.}
\label{fig:flowchart_motor}
\end{figure}



Der implementierte Algorithmus lässt sich in vier logische Hauptphasen unterteilen, die im Folgenden detailliert erläutert werden:
\begin{enumerate}
	\item \textbf{Signalerfassung:} Einlesen und Filtern der PWM-Signale vom Empfänger.
	\item \textbf{Normalisierung (Mapping):} Transformation der Zeitwerte (Mikrosekunden) in interne Steuergrößen.
	\item \textbf{Differential-Mixing:} Berechnung der individuellen Raddrehzahlen basierend auf dem Lenkwunsch.
	\item \textbf{Ausgabe und Sicherheit:} Begrenzung der Werte und Ansteuerung der Hardware-Treiber.
\end{enumerate}

\subsection{Detaillierte Implementierung}
\label{cha:Detaillierte Implementierung}

\subsubsection{Signalerfassung und Kanalzuordnung}
Die Fernsteuerung übermittelt Steuerbefehle als Pulsweiten-modulierte Signale (englisch: Pulse Width Modulation (PWM)), wobei die Information in der Länge des Pulses codiert ist. Das empfangene Signal variiert typischerweise zwischen \SI{1100}{\micro\second} und \SI{1900}{\micro\second}, wobei \SI{1500}{\micro\second} der Neutralstellung (Joystick mittig) entspricht (siehe Abbildung \ref{fig:joystick_pwm}).

\begin{figure}[h!]
	\centering
	\input{images/Joystick_y_achse.tex}
	\caption{Y-Achsenbewegung: PWM-Signalzuordnung bei der Fernsteuerung}
	\label{fig:joystick_pwm}
\end{figure}



Im ersten Schritt liest der Mikrocontroller diese Pulslängen mithilfe der Funktion \texttt{pulseIn()} ein. Softwareseitig erfolgt hierbei eine direkte Zuordnung der physikalischen Eingangspins zu den logischen Funktionen Fahrgeschwindigkeit (\textit{Speed}) und Lenkung (\textit{Steering}).


\begin{lstlisting}[style=motorStyle, caption={Einlesen und Zuordnen der RC-Signale}]
	// Auszug aus der Signalerfassung
	if (i == ENGINE) {
		// Der ENGINE-Kanal wird für die Lenkung (Rechts/Links) genutzt
		lastSteeringPulse = pulseLength_us;
	} else if (i == AILERON) {
		// Der AILERON-Kanal bestimmt die Geschwindigkeit (Vor/Zurück)
		lastSpeedPulse = pulseLength_us;
	}
\end{lstlisting}
Damit werden die eingelesenen Rohdaten der entsprechenden Empfängerkanäle direkt den internen Variablen für die Lenkung (\texttt{lastSteeringPulse}) und die Geschwindigkeit (\texttt{lastSpeedPulse}) zugewiesen.


\subsubsection{Transformation der Signale (Mapping)}
Ein entscheidender Schritt in der Software ist die Umrechnung der eingelesenen Zeitwerte in Werte, die die Motortreiber verarbeiten können. Die H-Brücken werden mit einem 8-Bit-PWM-Signal angesteuert (Wertebereich $0$ bis $255$), während die Fernbedienung Zeitwerte um $1500$ liefert.

Um dies zu lösen, wird die \texttt{map()}-Funktion eingesetzt, welche den Wertebereich der Eingabe linear auf den Zielbereich skaliert. Zusätzlich wurde eine sogenannte \textbf{Deadzone} (Totzone) zwischen \SI{1450}{\micro\second} und \SI{1550}{\micro\second} implementiert.

\begin{lstlisting}[style=motorStyle, caption={Mapping der Rohdaten mit Totzone}]
	int baseSpeed = 0;
	
	// Logik für die Geschwindigkeit
	if(lastSpeedPulse < 1450) {
		// Rückwärtsfahrt: Bereich 1100-1450 wird auf -255 bis 0 gemappt
		baseSpeed = map(lastSpeedPulse, 1100, 1450, -255, 0);
	} else if(lastSpeedPulse > 1550) {
		// Vorwärtsfahrt: Bereich 1550-1900 wird auf 0 bis 255 gemappt
		baseSpeed = map(lastSpeedPulse, 1550, 1900, 0, 255);
	} else {
		// Deadzone: In der Mittelstellung (Ruhelage) sicher stoppen
		baseSpeed = 0;
	}
\end{lstlisting}

Es gibt einen Grund, warum die Deadzone notwendig ist. Mechanische Joysticks kehren oft nicht mikrometergenau in die absolute Mitte zurück, und auch das Funksignal kann leicht schwanken (Jitter). Ohne diese implementierte Totzone würde der Roboter im Stillstand \glqq zittern\grqq ~oder langsam wegrollen. Die Deadzone garantiert, dass der Roboter sicher stehen bleibt, solange der Nutzer den Joystick nicht bewusst bewegt.

\subsubsection{Der Differential-Mixing-Algorithmus}
Da der Roboter über keine mechanische Lenkung wie ein Auto verfügt, muss eine Richtungsänderung durch unterschiedliche Drehzahlen der beiden Antriebsräder erzeugt werden. Diesen Vorgang nennt man \textit{Differential Steering}.

Der implementierte Algorithmus nimmt die Basisgeschwindigkeit (\texttt{baseSpeed}) und modifiziert diese für das linke und rechte Rad basierend auf dem Lenkeinschlag (\texttt{steering}).

\begin{lstlisting}[style=motorStyle, caption={Berechnung der differenziellen Geschwindigkeiten}]
	// Initial erhalten beide Motoren die gleiche Basisgeschwindigkeit
	int leftMotorSpeed = baseSpeed;
	int rightMotorSpeed = baseSpeed;
	
	if(steering > 0) {
		// Rechtskurve: Rechten Motor verlangsamen
		// Formel: Reduziere Geschwindigkeit um Prozentsatz des Lenkeinschlags
		rightMotorSpeed = baseSpeed * (100 - steering) / 100;
	} else if(steering < 0) {
		// Linkskurve: Linken Motor verlangsamen
		leftMotorSpeed = baseSpeed * (100 + steering) / 100;
	}
\end{lstlisting}

\textbf{Funktionsweise:} Wenn der Roboter geradeaus fährt (\texttt{baseSpeed} > 0) und nach rechts gelenkt wird (\texttt{steering} > 0), reduziert der Algorithmus die Geschwindigkeit des rechten Rades (kurveninneres Rad).
Zum Beispiel: Bei vollem Lenkeinschlag (\texttt{steering = 100}\footnote{nach Mapping}) wird der Faktor $(100 - 100) / 100 = 0$. Das rechte Rad stoppt, während das linke weiterdreht. Damit dreht der Roboter sich auf der Stelle um das rechte Rad. Dies ermöglicht enge Wendemanöver.

\textbf{Abweichung von der Planung:} In der ursprünglichen Planung war eine additive Geschwindigkeitsreduktion vorgesehen (siehe Abbildung \ref{fig:flowchart_steuerung}). Diese wurde jedoch nach ersten Tests verworfen, da sie bei niedriger Geschwindigkeit zu übermäßiger Bremsung führte, während bei hoher Geschwindigkeit die Lenkwirkung unzureichend war. Die maßgebliche Implementierung nutzt daher eine multiplikative Methode (\texttt{baseSpeed * (100 - steering) / 100}), welche die Reduktion proportional zur aktuellen Fahrgeschwindigkeit durchführt und somit ein konsistentes Lenkverhalten über alle Geschwindigkeitsbereiche gewährleistet.

\subsubsection{Sicherheitsbegrenzung und Hardware-Ansteuerung}
Bevor die berechneten Werte an die Hardware gesendet werden, durchlaufen sie eine Sicherheitsstufe. Hierbei wird die \texttt{constrain()}-Funktion genutzt, um sicherzustellen, dass keine Werte außerhalb des zulässigen Bereichs liegen. Zusätzlich wurde ein \texttt{batterie\_factor} von $0.91$ eingeführt.

\begin{lstlisting}[style=motorStyle, caption={Begrenzung der Ausgangsspannung}]
	const float batterie_factor = 0.91; 
	// Begrenzung auf ca. 90% der maximalen Leistung
	leftMotorSpeed = constrain(leftMotorSpeed, 
	-255 * batterie_factor, 
	255 * batterie_factor);
	
	rightMotorSpeed = constrain(rightMotorSpeed, 
	-255 * batterie_factor, 
	255 * batterie_factor);
\end{lstlisting}

Dieser Faktor dient dem Schutz der Motoren und der Elektronik vor Überspannung. Die verwendeten Motoren sind für eine maximale Betriebsspannung von \SI{12}{\volt} ausgelegt. Da jedoch ein vollgeladener 3S-LiPo-Akku eine Spannung von bis zu \SI{12,6}{\volt} liefert, muss das PWM-Signal entsprechend begrenzt werden, um die Motoren nicht zu beschädigen.

Das maximal zulässige Tastverhältnis (englisch: Duty Cycle) ergibt sich aus dem Verhältnis der maximalen Motorspannung zur Akkuspannung: 
\[
\text{Duty Cycle}_{\text{max}} = \frac{U_{\text{Motor, max}}}{U_{\text{Akku, max}}} = \frac{\SI{12}{\volt}}{\SI{12,6}{\volt}} \approx 0{,}95 .
\]

In der Implementierung wurde ein Sicherheitsfaktor von $0{,}91$ gewählt, was einem maximalen Tastverhältnis von ca. 91\,\% entspricht. Dies liegt deutlich unter dem theoretischen Maximum von $0{,}95$ und bietet zusätzlichen Spielraum für Spannungsschwankungen während des Betriebs sowie einen Sicherheitspuffer gegen kurzzeitige Überlastung. Diese konservative Begrenzung sorgt zudem für ein sanfteres Fahrverhalten und schützt sowohl die Motoren als auch die H-Brücken vor vorzeitigem Verschleiß.



Abschließend steuert die Logik die H-Brücken an. Da Gleichstrommotoren für die Drehrichtungsumkehr eine Umpolung benötigen, bietet der Motortreiber zwei Pins pro Motor (RPWM für Rechtslauf, LPWM für Linkslauf). Die Software stellt sicher, dass niemals beide Pins gleichzeitig aktiv sind, was einen Kurzschluss in der H-Brücke verhindern würde.

\begin{lstlisting}[style=motorStyle, caption={Ansteuerung der H-Brücke}]
	// Beispiel für den linken Motor
	if(leftMotorSpeed > 0) {
		// Vorwärtsfahrt
		analogWrite(PIN_BTS1_RPWM, abs(leftMotorSpeed)); 
		analogWrite(PIN_BTS1_LPWM, 0);
	} else if(leftMotorSpeed < 0) {
		// Rückwärtsfahrt
		analogWrite(PIN_BTS1_RPWM, 0);
		analogWrite(PIN_BTS1_LPWM, abs(leftMotorSpeed));
	} else {
		// Stopp (elektrische Bremse möglich durch HIGH/HIGH, hier Freilauf)
		analogWrite(PIN_BTS1_RPWM, 0);
		analogWrite(PIN_BTS1_LPWM, 0);
	}
\end{lstlisting}

\clearpage

\section{Display-Programmierung}
\label{cha:Display-Programmierung}

Für die Display-Programmierung wird ein Raspberry Pi 5 verwendet, wie im Vorgehen beschrieben. Dieser Einplatinencomputer eignet sich aufgrund seiner umfangreichen Schnittstellen, die in Abbildung \ref{fig:Aufbau RP5} dargestellt sind. 

Die Verkabelung und Datenflüsse sind in Abbildung \ref{fig:anschlussschema_rpi5}
Die gewählte Variante mit 8,GB RAM bietet ausreichend Rechenleistung, um neben der Display-Ansteuerung auch komplexere Aufgaben wie Objektverfolgung oder die Integration einfacher KI-Modelle zu ermöglichen.
Der Raspberry Pi 5 nutzt das Betriebssystem Raspberry Pi OS, eine auf Debian basierende Linux-Distribution, die speziell für den Raspberry Pi optimiert ist. Ein wesentlicher Vorteil dieser Plattform ist die integrierte GUI, die sowohl die Entwicklung als auch die Bedienung erleichtert. Dies ermöglicht nicht nur eine komfortable Programmierung, sondern auch eine einfache Handhabung durch Anwender ohne tiefgehende Linux-Kenntnisse. So können grundlegende Aufgaben wie das Starten des Systems oder die Fehlerdiagnose direkt über die grafische Oberfläche erfolgen.


Die HDMI- bzw. Micro-HDMI-Schnittstellen des Raspberry Pi 5 ermöglichen den Anschluss handelsüblicher Displays. Dadurch kann eine flexible und kostengünstige Lösung für die visuelle Ausgabe realisiert werden, ohne auf proprietäre Hardware angewiesen zu sein.

Für die Programmierung des Displays wird die Programmiersprache Python eingesetzt. Python bietet aufgrund ihrer umfangreichen Bibliotheken eine effiziente Möglichkeit zur Umsetzung grafischer und interaktiver Inhalte. Insbesondere die Bibliothek OpenCV ermöglicht die Verarbeitung von Kamerabildern, beispielsweise zur Realisierung von Augenbewegungen oder anderen bildbasierten Animationen. Für die Darstellung von grafischen Elementen und Animationen können Bibliotheken wie PyPlot und Pygame verwendet werden, die eine einfache Erstellung von visuellen Komponenten und dynamischen Effekten erlauben. Ein weiterer Vorteil von Python liegt in der hohen Verständlichkeit und der klaren Syntax, wodurch die Wartung und Fehlerbehebung auch für Anwender ohne tiefgehende Informatikkenntnisse erleichtert wird. Dies trägt zur langfristigen Nutzbarkeit und Erweiterbarkeit des Systems bei.

\subsection{Animations-Engine für Augen}
\label{cha:Animations-Engine für Augen}

Für die Animations Engine werden die Bibliotheken OpenCV, Pygame und der Kamerainput der Brio 500 Kamera verwendet. Auch dies kann man in Abbildung \ref{fig:anschlussschema_rpi5} erkennen. 

Allgemein soll die Grundanimation wie im Vorgehen schon erwähnt und in Abbildung \ref{fig:augenanimation_plain} zu sehen, eine einfache Animation basierend auf zwei Kreisen mit beweglichem Punkt in der Mitte.
Hier nochmal eine Grafik dazu:
\begin{figure}[ht]
	\centering
	\begin{tikzpicture}[font=\sffamily, scale=1.0]
		% Einfache Stile (nur Standard-TikZ)
		\tikzset{
			eye/.style={draw, thick, fill=white},
			iris/.style={draw, thick, fill=blue!20},
			pupil/.style={draw, thick, fill=black},
			boundary/.style={draw, dashed, gray!70, thick}
		}
		
		% Parameter (direkt gesetzt, ohne Rechenbibliotheken)
		\def\EyeR{2.0}      % Augenradius (Sklera)
		\def\IrisR{1.0}     % Irisradius
		\def\PupilR{0.45}   % Pupillenradius
		\def\MaxBound{0.65} % Begrenzungsfaktor relativ zur Iris
		
		% Augen-Zentren
		\coordinate (L) at (0,0);
		\coordinate (R) at (5,0);
		
		% Beispiel-Blickrichtung (direkte Verschiebung, ohne Berechnung)
		% Werte in etwa: dx, dy \in [-IrisR*MaxBound, IrisR*MaxBound]
		\def\mx{0.65}  % Verschiebung x
		\def\my{0.30}  % Verschiebung y
		
		% LINKES AUGE
		\draw[eye] (L) circle (\EyeR);                  % Sklera
		\draw[iris] (L) circle (\IrisR);                % Iris
		\draw[boundary] (L) circle (\MaxBound*\IrisR);  % Bewegungsgrenze
		\path (L) ++(\mx,\my) coordinate (LP);          % Pupillenposition (verschoben)
		\draw[pupil] (LP) circle (\PupilR);             % Pupille
		
		% RECHTES AUGE
		\draw[eye] (R) circle (\EyeR);
		\draw[iris] (R) circle (\IrisR);
		\draw[boundary] (R) circle (\MaxBound*\IrisR);
		\path (R) ++(\mx,\my) coordinate (RP);
		\draw[pupil] (RP) circle (\PupilR);
		
		% Einfache Beschriftungen (ohne calc)
		\node[font=\footnotesize] at (0,2.6)  {Linkes Auge};
		\node[font=\footnotesize] at (5,2.6)  {Rechtes Auge};
		\node[font=\footnotesize, align=center] at (0,-2.9)
		{Bewegungsgrenze \\ (\textit{MaxBound} \(\cdot\) Irisradius)};
		
		% Blickrichtungsvektor (optional, Standard-Pfeil)
		\draw[->, thick, red!70] (L) -- (LP);
		\draw[->, thick, red!70] (R) -- (RP);
	\end{tikzpicture}
	\caption{Grundanimation mit zwei Augen: Pupillen bewegen sich innerhalb einer begrenzten Zone (abhängig von \textit{MaxBound} und Irisradius) zur Simulation realistischer Augenbewegungen.}
	\label{fig:augenanimation_plain}
\end{figure}



Die Animations-Engine basiert auf einer Kombination aus Bildverarbeitung und grafischer Darstellung. Mithilfe von OpenCV wird die Position eines Objekts im Kamerabild ermittelt. Diese Position wird in eine relative Blickrichtung transformiert und auf die Pupillenbewegung übertragen. Die Pupillen werden innerhalb eines definierten Bereichs verschoben, um eine realistische Augenbewegung zu simulieren. Die grafische Darstellung erfolgt framebasiert mit Python-Bibliotheken wie Pygame, wodurch eine flüssige Animation gewährleistet wird. Zusätzlich können zeitgesteuerte Effekte wie Blinzeln oder Zustandsanzeigen integriert werden.

\begin{figure}[ht]
	\centering
	\begin{tikzpicture}[font=\sffamily]
		% Skalierungsfaktor nur für Nodes/Labels (Pfeile bleiben unskaliert)
		\def\nodescale{1.5}
		
		% Stile (Standard-TikZ)
		\tikzset{
			box/.style={draw, rounded corners, thick, fill=blue!7, minimum width=3.6cm, minimum height=0.9cm, align=center},
			smallbox/.style={draw, rounded corners, thick, fill=green!7, minimum width=3.4cm, minimum height=0.8cm, align=center},
			io/.style={draw, thick, fill=orange!15, rounded corners, minimum width=3.2cm, minimum height=0.8cm, align=center}
		}
		
		% ---------------------------
		% Nodes + Labels (größer)
		% ---------------------------
		\begin{scope}[transform shape, scale=\nodescale]
			% Vertikale Hauptkette
			\node[io]   (cam)  at (-1,  0.0) {Brio 500\\{\scriptsize RGB-Stream}};
			\node[box]  (cv)   at (-1, -1.6) {OpenCV\\{\scriptsize Erkennung \& Tracking}};
			\node[box]  (map)  at (-1, -3.2) {Mapping \& Filter\\{\scriptsize Normalisierung, Clamping}};
			\node[box]  (pg)   at (-1, -4.8) {Pygame Renderer\\{\scriptsize Frame-Zeichnung}};
			\node[io]   (disp) at (-1, -6.4) {Display/Panel\\{\scriptsize Augenanzeige}};
			
			% Seitliche Nebenpfade
			\node[smallbox] (fx)  at (-5, -3.2) {Effekte \& Zustände\\{\scriptsize Blinzeln, Status}};
			\node[smallbox] (tim) at ( 3.2, -4.8) {Timing \& Events\\{\scriptsize FPS, Timer, Loop}};
			
			% Kurze Hinweise (als Inhalt)
			\node at (-1,  0.9) {{\scriptsize USB, Auflösung, FPS}};
			\node at (-1, -0.5) {{\scriptsize Rauschen/Stabilität}};
			\node at (-1, -2.1) {{\scriptsize Begrenzter Pupillenradius}};
			\node at (-1, -5.7) {{\scriptsize Double Buffering}};
		\end{scope}
		
		% ---------------------------
		% Pfeile (NICHT skaliert)
		% ---------------------------
		\draw[->, thick] (cam) -- (cv);
		\draw[->, thick] (cv)  -- (map);
		\draw[->, thick] (map) -- (pg);
		\draw[->, thick] (pg)  -- (disp);
		
		\draw[->, thick] (fx.east)  -- (map.west);
		\draw[->, thick] (tim.west) -- (pg.east);
	\end{tikzpicture}
	\caption{Vertikale, kompakte Pipeline der Animations-Engine: Kamera \(\rightarrow\) OpenCV \(\rightarrow\) Mapping/Filter \(\rightarrow\) Pygame \(\rightarrow\) Anzeige. Effekte und Timing sind seitlich angebunden.}
	\label{fig:animations_pipeline_vertical_plain}
\end{figure}
\newpage

\subsection{Benutzerschnittstelle}
\label{cha:Benutzerschnittstelle}

Die Benutzerschnittstelle des Roboters ist ein zentraler Bestandteil für die Interaktion zwischen Benutzer und System. Sie umfasst sowohl lokale Hardware-Komponenten als auch Remote-Zugriffsmöglichkeiten. Im Folgenden werden die einzelnen Schnittstellen ausführlich beschrieben:

\begin{itemize}
	\item \textbf{Kamera (Logitech Brio 500):} 
	Die Kamera dient nicht nur zur Bildaufnahme, sondern auch als aktive Schnittstelle für Funktionen wie \textit{Objekterkennung} und \textit{Tracking}. Dadurch kann der Roboter auf Bewegungen oder Positionen reagieren und eine dynamische Interaktion mit dem Benutzer ermöglichen. Dies ist besonders für Anwendungen wie Blickrichtungssteuerung oder Gestenerkennung relevant.
	
	\item \textbf{Touchdisplay:} 
	Das verbaute HDMI-Display ist in der aktuellen Konfiguration als \textit{Touchscreen} ausgeführt. Es erlaubt direkte Eingaben wie Menünavigation, Starten von Funktionen oder Einstellen von Parametern. Der große Vorteil liegt in der intuitiven Bedienung ohne zusätzliche Peripherie wie Maus oder Tastatur. Für den Messebetrieb ist dies besonders praktisch, da Besucher direkt mit dem Roboter interagieren können.
	
	\item \textbf{Audioausgabe über USB-Lautsprecher:} 
	Ein externer USB-Lautsprecher ist integriert, um akustisches Feedback zu geben. Dies ermöglicht:
	\begin{itemize}
		\item Wiedergabe von Statusmeldungen (z.\,B. „System bereit“).
		\item Akustische Hinweise bei Benutzeraktionen.
		\item Optionale Sprachinteraktion oder Musik für Präsentationen.
	\end{itemize}
	Vorteile:
	\begin{itemize}
		\item Erhöhte Benutzerfreundlichkeit durch multisensorische Rückmeldung.
		\item Bessere Wahrnehmung in lauten Umgebungen (z.\,B. Messe).
		\item Möglichkeit zur Erweiterung um Sprachsteuerung.
	\end{itemize}
	
	\item \textbf{Remote-Zugriff (SSH / VNC):} 
	Für die Fernsteuerung des Raspberry Pi stehen zwei Hauptmethoden zur Verfügung:
	\begin{itemize}
		\item \textbf{SSH (Secure Shell):} 
		SSH ermöglicht eine sichere, textbasierte Verbindung zum Raspberry Pi. Damit können Konfigurationen vorgenommen, Programme gestartet oder Diagnosen durchgeführt werden. SSH ist ressourcenschonend und benötigt nur geringe Bandbreite. Es eignet sich ideal für Wartung und schnelle Eingriffe.
		
		\item \textbf{VNC (Virtual Network Computing):} 
		VNC erlaubt die Spiegelung des HDMI-Ausgangs auf ein anderes Gerät (Laptop, Tablet, Smartphone). Dadurch kann die grafische Oberfläche des Raspberry Pi remote bedient werden, als säße man direkt davor. VNC kann sogar als \textit{zweiter Desktop} genutzt werden, was zusätzliche Flexibilität für Präsentationen oder Messen bietet.
	\end{itemize}
	
	\textbf{Vorteile des Remote-Zugriffs:}
	\begin{itemize}
		\item Permanente Überwachung des Systems während des Einsatzes (z.\,B. auf Messen).
		\item Zugriff auf alle Funktionen ohne physische Nähe zum Roboter.
		\item Zusätzliche Features wie Fernauslösen der Kamera, Live-Streaming oder Upload von Medien.
		\item Möglichkeit zur schnellen Fehlerbehebung und Software-Updates.
	\end{itemize}
	
	\textbf{Nachteile:}
	\begin{itemize}
		\item Abhängigkeit von stabiler Netzwerkverbindung.
		\item Potenzielle Sicherheitsrisiken (erfordert Authentifizierung und ggf. VPN).
		\item Höhere Latenz bei grafischen Anwendungen im Vergleich zur lokalen Bedienung.
	\end{itemize}
\end{itemize}

\begin{figure}[ht]
	\centering
	\begin{tikzpicture}[font=\sffamily, scale=1.0]
		\tikzset{
			block/.style={draw, thick, rounded corners, fill=blue!10, minimum width=3.5cm, minimum height=1cm, align=center},
			io/.style={draw, thick, fill=orange!15, rounded corners, minimum width=3.2cm, minimum height=0.8cm, align=center}
		}
		
		% Zentrale Einheit
		\node[block] (rpi) at (0,0) {Raspberry Pi 5};
		
		% Direkte Schnittstellen
		\node[io] (cam) at (0,2.5) {Kamera\\{\scriptsize Objekterkennung}};
		\node[io] (touch) at (-4,-1.5) {Touchdisplay\\{\scriptsize Lokale Eingabe}};
		\node[io] (audio) at (4,-1.5) {USB-Lautsprecher\\{\scriptsize Audioausgabe}};
		
		% Remote Schnittstellen
		\node[io] (ssh) at (-4,3.5) {SSH / VNC\\{\scriptsize Remote-Steuerung}};
		\node[io] (web) at (4,3.5) {Web-Interface\\{\scriptsize Browser/App}};
		
		% Pfeile
		\draw[->, thick] (cam) -- (rpi);
		\draw[->, thick] (touch) -- (rpi);
		\draw[->, thick] (audio) -- (rpi);
		\draw[->, thick] (ssh) -- (rpi);
		\draw[->, thick] (web) -- (rpi);
		
	\end{tikzpicture}
	\caption{Benutzerschnittstellen des Roboters: Kamera, Touchdisplay und USB-Audio lokal; SSH/VNC und Web-Interface für Remote-Zugriff.}
	\label{fig:benutzerschnittstelle}
\end{figure}
\newpage

\section{Systemintegration und Test}
\label{cha:Systemintegration und Test}

\subsection{Integrationsstrategie}
\label{cha:Integrationsstrategie}

Die Systemintegration und die zugehörigen Testaktivitäten orientieren sich am im Projekt verwendeten V-Modell als systems-ingenieurwissenschaftliche Vorgehensweise. Entsprechend diesem Modell wurden die einzelnen Systembestandteile zunächst getrennt voneinander entwickelt und getestet, bevor sie schrittweise zu einem Gesamtsystem integriert wurden.

Zu Beginn der Entwicklung wurde das Gesamtsystem in klar abgegrenzte Module beziehungsweise Subsysteme unterteilt. Diese Module repräsentieren funktionale Einheiten des Roboters, die jeweils eine definierte Aufgabe erfüllen. Beispiele hierfür sind die Motoransteuerung, die Energieversorgung, die Steuerungslogik oder Anzeige- und Interaktionselemente. Durch diese Modularisierung konnte die Komplexität des Systems reduziert und die Entwicklung besser strukturiert werden.

Jedes Modul wurde zunächst unabhängig entwickelt und in Form von Einzeltests überprüft. Ziel dieser Tests war es, sicherzustellen, dass die jeweilige Funktion die zugehörigen Anforderungen aus dem Anforderungskatalog korrekt erfüllt. Die Tests wurden dabei gezielt auf die relevanten funktionalen und nicht-funktionalen Requirements ausgerichtet, wodurch eine direkte Rückverfolgbarkeit zwischen Anforderungen und Implementierung gewährleistet werden konnte.

Durch dieses Vorgehen konnten Fehler frühzeitig erkannt und lokalisiert werden, da sich Probleme eindeutig einem einzelnen Modul zuordnen ließen. Dies erleichterte die Fehlersuche erheblich und reduzierte den Aufwand für nachträgliche Korrekturen im Gesamtsystem. Gleichzeitig blieb der Überblick über den Entwicklungsstand der einzelnen Komponenten erhalten.

Nach erfolgreichem Abschluss der Einzeltests wurden die getesteten Module schrittweise in das Gesamtsystem integriert. In diesem Integrationsschritt wurde überprüft, ob die einzelnen Komponenten korrekt zusammenarbeiten und sich wie vorgesehen gegenseitig ergänzen. Auf diese Weise konnte das Gesamtsystem iterativ aufgebaut und abgesichert werden.

Dieses strukturierte Vorgehen aus modularer Entwicklung, modulbezogenen Tests und schrittweiser Integration trägt wesentlich zur Qualitätssicherung des Systems bei und entspricht dem grundlegenden Prinzip des V-Modells, bei dem jeder Entwicklungsstufe eine geeignete Verifikations- und Testaktivität zugeordnet ist.

\subsection{Subsystemtests des Fahr- und Antriebssystems}
\label{cha:Subsystemtests des Fahr- und Antriebssystems}

Auch wenn einzelne Module und Subsysteme für sich genommen die vorgesehenen Funktionen erfüllen, können im integrierten Betrieb weiterhin Fehler oder unerwartete Abweichungen auftreten. Diese entstehen häufig durch Integrationsprobleme, fehlerhaftes Verhalten an den Schnittstellen zwischen einzelnen Komponenten oder durch unzutreffende Annahmen über das Zusammenspiel der Teilsysteme. Zudem können logische Inkonsistenzen oder nicht berücksichtigte Randbedingungen erst im integrierten Betrieb sichtbar werden.

Aus diesem Grund ist es notwendig, neben Einzel- und Integrationstests auch umfassende Subsystemtests durchzuführen. Ziel dieser Testphase ist es, das Verhalten eines vollständig integrierten Funktionsbereichs unter realistischen Einsatzbedingungen zu überprüfen.

Im Rahmen dieses Projekts lag der Fokus der Subsystemtests auf dem Fahr- und Antriebssystem des Roboters. Dieses Subsystem umfasst unter anderem das Chassis, die Antriebseinheiten, die Motorsteuerung, die Energieversorgung sowie die Ansteuerung über die Fernbedienung. Getestet wurden insbesondere die Lenkung, das allgemeine Fahrverhalten, das Beschleunigen und Abbremsen sowie das Zusammenspiel der beteiligten Komponenten.

Die Tests wurden mit allen für den Fahrbetrieb relevanten Systemen in ihrer integrierten Form durchgeführt, um das reale Verhalten des Roboters möglichst genau abzubilden. Auf diese Weise konnten Wechselwirkungen zwischen Elektronik und Steuerungslogik gezielt beobachtet und bewertet werden.

Ein wesentlicher Vorteil dieser ganzheitlichen Tests auf Subsystemebene besteht darin, dass Schwachstellen oder Probleme identifiziert werden können, die während der Anforderungsdefinition oder der modularen Entwicklung nicht vorhersehbar waren. Insbesondere das Zusammenspiel mehrerer Komponenten kann zu Effekten führen, die sich erst im praktischen Betrieb zeigen, beispielsweise unerwartete Reaktionen bei Richtungswechseln, Verzögerungen in der Steuerung oder Abweichungen im Fahrverhalten.

Die Ergebnisse der Subsystemtests liefern somit wichtige Erkenntnisse darüber, wie sich die theoretisch geplanten Konzepte und technischen Lösungen in der praktischen Umsetzung bewähren. Diese Testphase stellt einen zentralen Bestandteil der Qualitätssicherung dar und bildet die Grundlage für gezielte Anpassungen und Optimierungen des Fahr- und Antriebssystems.

\subsubsection{Fehleranalyse und Optimierung}

Im Anschluss an die durchgeführten Subsystemtests wurden die beobachteten Ergebnisse analysiert und potenzielle Schwachstellen identifiziert. Ziel dieser Fehleranalyse ist es, Optimierungspotenziale für die weitere Entwicklung des Fahr- und Antriebssystems abzuleiten.

Die Tests zeigten, dass sich das Fahrzeug in der aktuellen Umsetzung intuitiv und leicht verständlich steuern lässt. Das Fahrverhalten wird insgesamt als flüssig und stabil wahrgenommen. Einen wesentlichen Beitrag dazu leistet das einzelne, frei bewegliche Vorderrad, das eine sanfte und kontinuierliche Richtungsänderung ermöglicht. Auch Richtungswechsel, bei denen sich das Vorderrad um bis zu 180° neu ausrichten muss, erfolgen ohne abrupte Bewegungen. Dies ist unter anderem auf die vergleichsweise große Dimensionierung des Rades mit einem Durchmesser von \SI{10}{\centi\meter} zurückzuführen.

\begin{figure}[hbt]
	\centering
	\fbox{%
		\input{images/Räder_Anordnung_Test.tex}
	}
	\caption{Zeigt die aktuelle Anordnung des Frontrades und die Optimierungsmöglichkeit aus der Draufsicht}		
	\label{fig:Räder_Anordnung_Test}
\end{figure}

Auch auf glatten Untergründen, wie sie typischerweise in Messehallen vorkommen, konnte sich das Fahrwerk bewähren. Die Lastverteilung auf drei Räder führt offenbar zu einem erhöhten Anpressdruck pro Rad, was sich positiv auf die Haftung auswirkt und das Auftreten von Schlupf reduziert. Gleichzeitig zeigte sich jedoch, dass die geometrische Anordnung der drei Räder welches kein ideales gleichseitiges Dreieck bildet zu einer schlechteren Kippstabilität führt. Der Abstand zwischen dem vorderen Rad und den beiden hinteren Rädern ist kürzer, was offenbar zu einer ungleichmäßigen Belastung und zu Stabilitätsnachteilen führt. Dieser Aspekt stellt einen Ansatzpunkt für eine mögliche geometrische Optimierung des Fahrwerks dar. Dabei müsste Das Vorderrad um den nötigen Abstand weiter nach vorne montiert werden, wie in Abbildung \ref{fig:Räder_Anordnung_Test} dargestellt.



Während der Testfahrten trat zudem ein sicherheitsrelevantes Problem auf. Durch Erschütterungen lösten sich einzelne elektrische Leitungen von der Steuerungselektronik, was in der Folge zu unkontrollierten Zuständen der Motoren führte. Diese Beobachtung ist insbesondere im Hinblick auf den vorgesehenen Messebetrieb kritisch, da unkontrollierte Bewegungen in Bereichen mit hohem Personenaufkommen ein erhebliches Sicherheitsrisiko darstellen.

Aus dieser Erkenntnis ergab sich ein unmittelbarer Handlungsbedarf auf zwei Ebenen: Zum einen musste die mechanische Fixierung und Zugentlastung der Leitungen verbessert werden, um das Risiko von Kontaktverlusten durch Erschütterungen zu minimieren. Zum anderen -- und dies erwies sich als noch kritischer -- offenbarte die Codeanalyse eine grundlegende Schwachstelle in der Steuerungslogik selbst, die unabhängig von der physischen Ursache des Signalverlusts eine Gefährdung darstellte. Die Implementierung eines robusten Failsafe-Mechanismus wurde daher zu einer zentralen Priorität der Systemoptimierung.

Die durchgeführte Fehleranalyse verdeutlicht, dass Subsystemtests nicht nur der Funktionsprüfung dienen, sondern wertvolle Erkenntnisse über reale Betriebsbedingungen liefern. Die identifizierten Schwachstellen bilden eine wichtige Grundlage für gezielte technische und konzeptionelle Optimierungen des Systems.


\subsubsection{Implementierung eines Failsafe-Mechanismus}

Die während der Testfahrten aufgetretenen unkontrollierten Fahrzeugbewegungen machten deutlich, dass die ursprüngliche Implementierung der Motorsteuerung keine ausreichende Absicherung gegen Signalverluste besaß. Eine detaillierte Codeanalyse offenbarte die Ursache dieses sicherheitskritischen Verhaltens.

\paragraph{Problemanalyse}
In der ursprünglichen Implementation wurden die Steuersignale für Geschwindigkeit und Lenkung in statischen Variablen gespeichert:

\begin{lstlisting}[style=motorStyle, caption={Problematische Signalspeicherung ohne Validierung}]
	static int lastSteeringPulse = 1500;
	static int lastSpeedPulse = 1500;
	
	// Signaleinlesen in der Hauptschleife
	if(pulseLength_us != 0){ 
		if(i == ENGINE){
			lastSteeringPulse = pulseLength_us;
		}
		else if(i == AILERON){
			lastSpeedPulse = pulseLength_us;
		}
	}
\end{lstlisting}

Das fundamentale Problem dieser Implementierung liegt in der Verwendung von \texttt{static}-Variablen ohne Gültigkeitsprüfung. Statische Variablen behalten ihre Werte über mehrere Funktionsaufrufe hinweg bei. Wenn das Eingangssignal aufgrund einer gelösten Leitung oder eines Empfängerausfalls wegfällt, gibt die \texttt{pulseIn()}-Funktion zwar den Wert 0 zurück, jedoch werden die gespeicherten Werte \texttt{lastSteeringPulse} und \texttt{lastSpeedPulse} nicht aktualisiert. Die Motorsteuerung verwendet somit weiterhin die zuletzt empfangenen gültigen Werte und führt die Fahrt mit unveränderter Geschwindigkeit und Richtung fort -- ohne jegliche Möglichkeit zur Intervention durch den Bediener.

Dieses Verhalten stellt ein erhebliches Sicherheitsrisiko dar, insbesondere im vorgesehenen Messebetrieb mit hohem Personenaufkommen. Ein Fahrzeug, das sich unkontrolliert weiterbewegt, kann zu Kollisionen mit Personen oder Ausstellungsobjekten führen.

\paragraph{Lösungsansatz: Failsafe-Mechanismus}
Zur Behebung dieser Schwachstelle sollte ein mehrstufiger Failsafe-Mechanismus implementiert werden, der auf drei Säulen basiert:

\begin{enumerate}
	\item \textbf{Signalvalidierung}: Kontinuierliche Überprüfung der Gültigkeit eingehender Steuersignale
	\item \textbf{Timeout-Überwachung}: Zeitbasierte Erkennung von Signalverlusten
	\item \textbf{Sicherer Systemzustand}: Automatisches Überführen der Motoren in einen definierten Stopp-Zustand
\end{enumerate}

Eine mögliche Implementierung würde die Steuerungslogik um einen Zeitstempel erweitern, der bei jedem gültigen Signalempfang aktualisiert wird:

\begin{lstlisting}[style=motorStyle, caption={Vorschlag für erweiterte Signalverarbeitung mit Gültigkeitsprüfung}]
	static int lastSteeringPulse = 1500;
	static int lastSpeedPulse = 1500;
	static uint32_t lastValidSignalTime = 0;
	const uint32_t SIGNAL_TIMEOUT_MS = 500;
	
	bool validSteeringSignal = false;
	bool validSpeedSignal = false;
	
	// In der Signalverarbeitungsschleife
	if(pulseLength_us != 0){ 
		if(i == ENGINE){
			lastSteeringPulse = pulseLength_us;
			validSteeringSignal = true;
		}
		else if(i == AILERON){
			lastSpeedPulse = pulseLength_us;
			validSpeedSignal = true;
		}
		// Weitere Verarbeitung...
	}
	
	// Timer-Update nur bei gültigen Signalen
	if(validSteeringSignal && validSpeedSignal) {
		lastValidSignalTime = millis();
	}
\end{lstlisting}

Der kritische Aspekt dieser Lösung wäre die explizite Unterscheidung zwischen dem Vorhandensein eines Signals (\texttt{pulseLength\_us != 0}) und der Aktualisierung des Zeitstempels. Nur wenn beide erforderlichen Signalkanäle (Lenkung und Geschwindigkeit) gültige Werte liefern, würde der Zeitstempel erneuert.

Vor der eigentlichen Motoransteuerung müsste eine Timeout-Prüfung erfolgen:

\begin{lstlisting}[style=motorStyle, caption={Vorschlag für Failsafe-Aktivierung bei Signalverlust}]
	if(millis() - lastValidSignalTime > SIGNAL_TIMEOUT_MS) {
		// Alle Motoren sofort stoppen
		analogWrite(PIN_BTS1_RPWM, 0);
		analogWrite(PIN_BTS1_LPWM, 0);
		analogWrite(PIN_BTS2_RPWM, 0);
		analogWrite(PIN_BTS2_LPWM, 0);
		
		Serial.println("FAILSAFE: Kein Signal - Motoren gestoppt!");
		return;
	}
	
	// Normale Motorsteuerung wird nur ausgefuehrt, 
	// wenn Failsafe nicht aktiv ist
	int baseSpeed = 0;
	// ... weitere Steuerungslogik
\end{lstlisting}

Die gewählte Timeout-Dauer von \SI{500}{\milli\second} würde einen Kompromiss zwischen Reaktionsgeschwindigkeit und Störfestigkeit darstellen. Sie wäre kurz genug, um bei einem tatsächlichen Signalverlust schnell zu reagieren, aber lang genug, um kurzzeitige Signalstörungen oder Verzögerungen in der Signalverarbeitung nicht fälschlicherweise als Fehler zu interpretieren.

\paragraph{Zusätzliche Absicherungsmaßnahmen}
Ergänzend zur Timeout-Überwachung sollte eine Plausibilitätsprüfung der empfangenen Pulslängen implementiert werden:

\begin{lstlisting}[style=motorStyle, caption={Vorschlag für Validierung der Signalplausibilität}]
	// Unrealistische Werte abfangen
	if(pulseLength_us < 900 || pulseLength_us > 2100) {
		pulseLength_us = 0;  // Als ungueltig markieren
	}
\end{lstlisting}

Diese Prüfung würde Messwerte herausfiltern, die außerhalb des physikalisch möglichen Bereichs für PWM-Steuersignale liegen. Typische RC-Empfänger generieren Pulse zwischen \SI{1000}{\micro\second} und \SI{2000}{\micro\second}. Werte außerhalb des erweiterten Bereichs von \SI{900}{\micro\second} bis \SI{2100}{\micro\second} würden auf Messfehler oder elektrische Störungen hindeuten und sollten als ungültig verworfen werden.

\paragraph{Bewertung der Lösung}
Die vorgeschlagene Failsafe-Logik würde das identifizierte Sicherheitsproblem auf mehreren Ebenen adressieren. Sie könnte Signalverluste zuverlässig erkennen und das System innerhalb von \SI{500}{\milli\second} in einen sicheren Zustand überführen. Die zusätzliche Plausibilitätsprüfung würde die Robustheit gegenüber elektrischen Störungen und fehlerhaften Messwerten erhöhen.

Für den praktischen Messebetrieb wäre eine Erweiterung um eine optische oder akustische Warnmeldung sinnvoll, die den Bediener über den aktivierten Failsafe-Zustand informiert. Dies könnte beispielsweise durch schnelles Blinken der Autopilot-LED oder einen Signalton realisiert werden.

Die vorgeschlagene Optimierung verdeutlicht die Notwendigkeit, bereits in der Entwicklungsphase potenzielle Fehlerzustände systematisch zu analysieren und entsprechende Absicherungsmechanismen vorzusehen. Insbesondere bei autonomen oder ferngesteuerten Systemen, die in der Nähe von Personen operieren, müssen Fail-Safe-Konzepte integraler Bestandteil der Steuerungslogik sein.

\subsection{Subsystemtests der Augenansteurung über Display und Kamera}
\label{cha:Subsystemtests der Augenansteurung über Display und Kamera}
Für die Systemtests  werden zuerst die einzelnen Komponenten einzeln wie unten verbunden und getestet. Danach folgt die Integration.
\subsection{Integration}
\label{cha:Integration}

Die Integration erfolgt in mehreren Phasen:
\begin{itemize}
	\item \textbf{Phase~1 - Hardware-Basisprüfung:} Einzeltests von Raspberry~Pi~5, Kamera (Logitech Brio~500), HDMI‑Touchdisplay, USB‑Lautsprecher sowie Verkabelung und Stromversorgung.
	\item \textbf{Phase~2 - Software-Grundlagen:} Installation und Konfiguration der Python‑Anwendung inkl. OpenCV/Pygame sowie Basistests (Starten, einfache Anzeige, Audio‑Testton).
	\item \textbf{Phase~3 - Modul‑Integration:} Zusammenführung von Kamerastream, Bildverarbeitung (Tracking), Pupillen‑/Augenanimation, Touch‑Eingaben und Audiofeedback.
	\item \textbf{Phase~4 - Systemtest:} End‑to‑End‑Szenarien (z.\,B. „Besucher vor Kamera \(\rightarrow\) Blickrichtungsanimation \(\rightarrow\) Touch‑Menü \(\rightarrow\) Audiofeedback“).
	\item \textbf{Phase~5 - Stresstest/Dauerlauf:} Langzeittest unter typischer Last (Animation, Kamerastream, Audiohinweise), Beobachtung von Temperatur, Last und Stabilität.
\end{itemize}

\subsection{Testumgebung und Aufbau}
\label{cha:Testumgebung und Aufbau}

\begin{itemize}
	\item \textbf{Hardware:} Raspberry~Pi~5, HDMI‑Touchdisplay (Strom via USB), Logitech Brio~500 (USB~3.0/UVC), USB‑Lautsprecher (UAC), Verkabelung (USB‑C Power, HDMI, USB).
	\item \textbf{Stromversorgung:} Netzteil 5\,V / 5\,A (USB‑C) und \textit{alternativ} Powerbank (mobil).
	\item \textbf{Software:} Python‑Anwendung mit OpenCV/Pygame, Konfigurationsdateien/Parameter (Auflösung, FPS, Audio‑Volume).
	\item \textbf{Umgebungsbedingungen:} Labor (kontrolliert) und angestrebte Messebedingungen (Publikumsverkehr, Umgebungsgeräusche, wechselnde Lichtverhältnisse).
\end{itemize}

\subsection{Testfälle und Akzeptanzkriterien}
\label{cha:Testfälle und Akzeptanzkriterien}

\begin{itemize}
	\item \textbf{Funktionstests:} Kamera liefert stabile Frames, Touch registriert Eingaben, Audioausgabe ist klar und laut genug, Display zeigt flüssig an.
	\item \textbf{Integrationstests:} Positions‑/Trackingdaten werden korrekt auf die Augenanimation gemappt.
	\item \textbf{Systemtests:} End‑zu‑End‑Szenarien laufen ohne Ausfälle; Bedienfluss ist intuitiv; Reaktionszeiten liegen im geforderten Bereich.
	\item \textbf{Stresstests/Dauerlauf:} Keine Instabilität über mehrere Stunden; Temperaturen bleiben im unkritischen Bereich; Framerate und Eingabelatenz stabil.
\end{itemize}

\subsection{Beobachtungen und Probleme}
\label{cha:Beobachtungen und Probleme}

\textbf{Stromversorgung mit Powerbank:} Während der Tests ist ein Problem bei der Stromversorgung des Raspberry~Pi mit angeschlossener Peripherie (Kamera, Touchdisplay, USB‑Lautsprecher) aufgetreten. Beim Betrieb über eine Powerbank traten \textit{Unterspannungen/Brownouts} und sporadische Neustarts auf.
\begin{itemize}
	\item \textbf{Vermutete Ursache:} Kurzfristige Lastspitzen (v.\,a. Kamera/Display) überschreiten die Leistungsabgabe der Powerbank; Spannungsabfall bei Leitungs-/Steckerverlusten.
	\item \textbf{Wirkung:} Instabiler Betrieb, Aussetzer beim Videostream, Audio‑Knacken, gelegentliche Reboots.
	\item \textbf{Empfehlung:}
	\begin{itemize}
		\item Powerbank/Netzteil mit \(\geq\)5\,V und \(\geq\)5\,A \textit{echter} Dauerlast; ideal mit USB‑PD‑Profilen passend zum Raspberry Pi~5.
		\item Kurze, hochwertige Kabel (USB‑C/HDMI/USB), ggf. \(\leq\)0{,}5\,m.
		\item Option: Separates Netzteil für Display (\emph{Last entkoppeln}).
		\item Optionaler \emph{Power‑Budget‑Check}: Messung von durchschnittlichem und Peak‑Strom unter realen Szenarien.
	\end{itemize}
\end{itemize}

\subsection{Geplante Netzwerktests (SSH/VNC)}
\label{cha:Geplante Netzwerktests (SSH/VNC)}

Die Netzwerktests sind vorgesehen, aber zum aktuellen Zeitpunkt \textbf{noch nicht durchgeführt}. Geplant sind:
\begin{itemize}
	\item \textbf{SSH (Secure Shell):}
	\begin{itemize}
		\item Verbindungsaufbau im lokalen Netz; Authentifizierung per Schlüssel.
		\item Befehle: Systemstatus (CPU/RAM/Temp), Start/Stop der Anwendung, Log‑Abruf.
		\item \textbf{Akzeptanzkriterien:} Verbindung innerhalb \(\leq\)2\,s aufgebaut; Latenz für Kommandoausführung \(\leq\)300\,ms; Stabilität über 2\,h Dauerverbindung.
	\end{itemize}
	\item \textbf{VNC (Virtual Network Computing):}
	\begin{itemize}
		\item Spiegelung des HDMI‑Desktops auf Laptop/Tablet/Smartphone; Test als \emph{zweiter Desktop}.
		\item Szenario: Fernbedienung im Messebetrieb (Menübedienung, Live‑Monitoring).
		\item \textbf{Akzeptanzkriterien:} Bedienbar bei \(\geq\)10\,FPS Remote‑GUI, Eingabelatenz \(\leq\)200\,ms bei 2{,}4/5\,GHz WLAN; keine Abbrüche über 2\,h.
	\end{itemize}
\end{itemize}

\subsection{Fehleranalyse und Optimierung}
\label{cha:Fehleranalyse und Optimierung}

Die Fehleranalyse basiert auf den Beobachtungen während der Integration und den Stresstests:
\begin{itemize}

	\item \textbf{Hardware:}
\end{itemize}
	\begin{itemize}
		\item \emph{Stromversorgung:} Unterspannung durch Powerbank → Empfehlung: Netzteil mit stabiler 5\,A-Leistung oder Powerbank mit USB-PD-Profil.
	\end{itemize}


%----------------------------------------------------------------------------------------------------


\chapter{Ergebnisse und Diskussion}
\label{cha:Ergebnisse und Diskussion}

In diesem Kapitel werden die realisierten Ergebnisse der Studienarbeit vorgestellt
und kritisch bewertet. Dabei liegt der Fokus auf dem Vergleich zwischen den definierten Anforderungen und dem tatsächlich umgesetzten Funktionsstand des Roboters.

\section{Funktionsnachweis und Leistungsbewertung}
\label{cha:Funktionsnachweis und Leistungsbewertung}

Zum Abschluss der Entwicklungsphase wurde der Roboter einer Reihe von Funktionstests unterzogen, um die Leistungsfähigkeit der Teilsysteme sowie des Gesamtsystems zu validieren.

\subsection{Antrieb und Fahrverhalten}
\label{cha:Antrieb und Fahrverhalten}

Das Chassis des Roboters wurde als stabile Holzkonstruktion realisiert, in welche die Antriebseinheit integriert ist. Diese besteht aus zwei unabhängig angesteuerten Gleichstrommotoren (DC-Motoren) für den Vortrieb sowie einem frei beweglichen Stützrad, das für die nötige Stabilität sorgt. Die Tests bestätigten die Funktionsfähigkeit der implementierten Motorsteuerung:

\begin{itemize}
	\item \textbf{Fernsteuerung:} Der Roboter lässt sich zuverlässig von außen kontrollieren. Die Übertragung der Steuerbefehle für Lenkung und Geschwindigkeit erfolgt latenzarm.
	
	\item \textbf{Manövrierfähigkeit:} Durch die differenzielle Ansteuerung ist ein sanftes Kurvenfahren sowie ein Drehen auf der Stelle möglich. Das frei bewegliche Stützrad folgt dabei der Bewegungsrichtung ohne nennenswerten Widerstand.
	
	\item \textbf{Geschwindigkeitsregelung:} Die Geschwindigkeit lässt sich stufenlos variieren, was ein präzises Navigieren auch bei niedrigen Geschwindigkeiten ermöglicht.
\end{itemize}

\subsection{Interaktion und Aktorik}
\label{cha:Interaktion und Aktorik}

Die "`Persönlichkeit"' des Roboters wird erfolgreich durch die implementierten audiovisuellen Komponenten vermittelt:

\begin{itemize}
\item \textbf{Visuelle Ausgabe:} Die Augen-Animationen auf dem Display-Kopf funktionieren wie geplant und können Humankörper verfolgen.
	
	\item \textbf{Akustische Ausgabe:} Das System kann definierte Roboter-Geräusche abspielen, was die Interaktion lebendiger gestaltet.
	
\end{itemize}

\subsection{Systemintegration}
\label{cha:Systemintegration}

Der Körper des Roboters (\textit{Body}) wurde vollständig aufgebaut und beherbergt die gesamte Antriebstechnik sowie die Energieversorgung. Die mechanische und elektrische Integration der Kopf-Einheit auf den Körper steht als finaler Arbeitsschritt noch aus. Die notwendigen Schnittstellen sind jedoch bereits definiert und vorbereitet.

\section{Anforderungsabgleich}
\label{cha:Anforderungsabgleich}

Alle in der Anforderungsliste (siehe Kapitel~\ref{sec:Projekt Requirements}) definierten Anforderungen wurden erfolgreich umgesetzt, mit Ausnahme der folgenden Punkte:

\textbf{Fehlende Kopf-Integration (REQ-007, REQ-010, REQ-011, REQ-012, REQ-105):} Ein Großteil der noch offenen Anforderungen resultiert daraus, dass die finale Integration der Kopf-Einheit auf den Roboterkörper noch nicht vollzogen wurde. Da das Display für Statusanzeigen (REQ-007) im Kopf verbaut ist, besteht aktuell keine physische Datenverbindung zum Akku im Rumpf. Ebenso sind alle Anforderungen, die eine Drehung des Kopfes beinhalten, technisch vorbereitet, konnten aber mangels Montage noch nicht im Gesamtsystem demonstriert werden.

\textbf{Sicherheitsfunktionen (REQ-201, REQ-203, REQ-205):} Die Sicherheit hatte eine hohe Priorität, konnte jedoch in der gegebenen Zeit nicht vollständig validiert werden. Besonders REQ-201 (Failsafe bei Signalverlust) zeigte in Tests unerwartetes Verhalten: Während der Software-Timeout korrekt funktionierte, führte ein Stromausfall am Empfänger selbst dazu, dass der Motortreiber das letzte gültige Signal beibehielt (Last-State-Hold), statt zu stoppen. Dieses Verhalten muss in einer zukünftigen Iteration hardwareseitig abgefangen werden. Auch der mechanische Schutz der Elektronik vor Erschütterungen (REQ-205) sowie das sensorgestützte automatische Stoppen (REQ-203) stehen noch auf der Agenda für die nächste Entwicklungsphase.

\textbf{Erweiterte Funktionen (REQ-501, REQ-009):} Die Implementierung von KI-Funktionen (REQ-009) und die Anpassung an das spezifische DHBW-Design (REQ-501) wurden zugunsten einer stabilen Basisfunktionalität (Fahren und Steuern) zurückgestellt. Der aktuelle Holz-Prototyp dient als funktionaler Träger, dessen Design in späteren Schritten verfeinert wird.

\section{Lessons Learned}
\label{cha:Lessons Learned}

\begin{itemize}
	\item \textbf{Frühzeitige Beschaffung:} Die späte Bestellung von Bauteilen führte zu Verzögerungen im Projektablauf. Dadurch verringerte sich die verfügbare Zeit für ausgiebige Tests, wodurch Sicherheitslücken (wie das Verhalten bei Stromausfall des Empfängers) erst spät erkannt wurden. Eine frühzeitige Materialbeschaffung ist essenziell ("`Frontloading"').
	
	\item \textbf{Gemeinsames Requirements Engineering:} Anforderungen sollten stets gemeinsam im Team (Brainstorming) definiert werden. Getrennte Überlegungen führten initial zu Redundanzen und Unklarheiten, die später konsolidiert werden mussten.
	
	\item \textbf{Klare Aufgabenverteilung:} Eine präzise Definition von Verantwortlichkeiten ist notwendig, um selbstständiges Arbeiten zu fördern. Unklare Zuständigkeiten führten phasenweise zu Ineffizienzen, da Teammitglieder auf Input warteten.
	
	\item \textbf{Regelmäßige Meetings:} Die Einführung eines wöchentlichen Jour fixe zur selben Uhrzeit könnte als kritischer Erfolgsfaktor dienen. Im Projektverlauf wurde deutlich, dass Kommunikationslücken bestanden und Fortschritte sowie Unterstützungsbedarf nicht rechtzeitig kommuniziert wurden. Regelmäßige Abstimmungen hätten diese Probleme frühzeitig aufdecken können.
\end{itemize}

\section{Kritische Reflexion}
\label{cha:Kritische Reflexion}

Die Entwicklung des Roboters war für unser Projektteam eine vielseitige Herausforderung. Rückblickend lässt sich das Ergebnis am besten in drei Bereichen bewerten: Technik, Methodik (Arbeitsweise) und Zeitmanagement. Technisch haben wir unser Hauptziel erreicht: Ein funktionierendes Grundsystem wurde fertiggestellt, das sich sicher bewegen und grundlegend interagieren kann. Die Entscheidung, den Antrieb über einen ESP32 zu steuern, hat sich als sehr zuverlässig erwiesen und sorgt für eine gute Manövrierfähigkeit. Die Aufgabenteilung war ebenfalls korrekt, wo der ESP32 für den Antrieb zuständig ist, während der Raspberry Pi die Interaktion übernimmt. Dadurch haben wir Ressourcenkonflikte vermieden. Allerdings gibt es Unterschiede zwischen unserem ursprünglichen Plan und dem jetzigen Prototypen. Da die Zeit knapp wurde, konnten wir den Kopf des Roboters mechanisch noch nicht vollständig integrieren. Die Funktionen für die Kopf-Drehung sind zwar technisch vorbereitet, können aber am Gesamtsystem noch nicht vorgeführt werden.

Ein wichtiger Punkt ist die Sicherheit. Softwareseitig funktionieren unsere Schutzmechanismen, wie zum Beispiel die definierten Totzonen („Deadzones“), sehr gut. In den Hardware-Tests zeigten sich jedoch Schwächen: Wenn der Empfänger den Strom verliert, behalten die Motortreiber das letzte Signal bei („Last-State-Hold“), anstatt den Roboter sofort zu stoppen. Dieses Risiko hatten wir in der Planungsphase nicht vorgesehen. Auch beim Gehäuse mussten wir Abstriche machen. Der Roboter besteht aktuell noch aus seinem hölzernen Grundgerüst. Die geplante Verkleidung aus dem 3D-Drucker haben wir zugunsten der Stabilität erst einmal zurückgestellt. Technisch funktioniert der Roboter zwar einwandfrei, aber für einen professionellen Auftritt auf einer Messe fehlt noch die passende Optik.

Auch unsere Arbeitsweise liefert wichtige Erkenntnisse. Wir haben uns für eine Mischung aus dem strukturierten V-Modell und agilen Methoden entschieden. Das hatte Vor- und Nachteile. Einerseits konnten wir durch die agile Arbeitsweise schnell reagieren. Andererseits fehlten uns zu Beginn feste Abstimmungsrituale im Team. Das führte dazu, dass wir Probleme an den Schnittstellen zwischen Mechanik und Elektronik teilweise zu spät bemerkt haben, was am Ende den Aufwand beim Zusammenbau erhöhte. Eine der wichtigsten Lehren für uns ist deshalb: Anforderungen müssen von Anfang an gemeinsam im gesamten Team besprochen und festgelegt werden, damit alle vom selben Stand ausgehen.

Beim Zeitmanagement war vor allem die Materialbeschaffung ein Engpass. Da wichtige Bauteile wie Motortreiber und Akkus verspätet geliefert wurden, blieb uns am Ende viel weniger Zeit zum Testen als geplant. Das bestätigt, wie wichtig es ist, kritische Komponenten sofort zu Projektbeginn zu bestellen und zu prüfen. Zusammenfassend lässt sich sagen: Der Roboter ist eine solide Plattform und der „Proof of Concept“ ist geglückt. Damit er jedoch wirklich auf einer Messe eingesetzt werden kann, ist eine zweite Entwicklungsrunde nötig, um die Sicherheitslücken zu schließen und das Design fertigzustellen.

\section{Mögliche Erweiterungen}
\label{cha:Mögliche Erweiterungen}

Die modulare Architektur der Motorsteuerung erlaubt prinzipiell eine nahezu unbegrenzte Erweiterung des Funktionsumfangs. Im Hinblick auf die technische Realisierbarkeit in der nahen Zukunft stehen jedoch folgende Optionen im Fokus, um den Einsatzbereich des Roboters sinnvoll zu vergrößern:

\begin{itemize}
	\item \textbf{Aktiver Notbremsassistent:} Durch die Integration von Umfeldsensorik (z. B. Ultraschall) kann eine Sicherheitsfunktion implementiert werden, die den Roboter bei Hinderniserkennung automatisch stoppt, um Kollisionen zu verhindern.
	
	\item \textbf{Mensch-Maschine-Interaktion mittels lokaler KI:} Durch den Einsatz kompakter Sprachmodelle (Small Language Models), die lokal auf dem Bordcomputer ausgeführt werden, kann eine natürliche Interaktion mit dem Menschen ermöglicht werden. Dies umfasst das Verstehen von Sprachbefehlen sowie die Generierung von Antworten, ohne auf eine permanente Internetverbindung angewiesen zu sein.
	
	\item \textbf{Autonomes Fahren mittels Kameratechnik:} Die Anbindung eines Kamerasystems ermöglicht die Erweiterung um bildverarbeitende Algorithmen. Damit können Funktionen wie Spurverfolgung (Line Tracking) oder visuelle Navigation realisiert werden, um autonom von einem Start- zu einem Zielpunkt zu navigieren.
	
	\item \textbf{Remote-Steuerung und Telemetrie:} Alternativ zur lokalen Signalverarbeitung kann eine Schnittstelle für drahtlose Kommunikation (WLAN/Bluetooth) geschaffen werden. Dies ermöglicht eine Fernsteuerung über mobile Endgeräte sowie die Rückübertragung von Telemetriedaten (z. B. Akkustand, Motorauslastung) an den Bediener.
	
	\item \textbf{Arm-Module:} Passend zur Inspirationsquelle Wall-E\reg können bewegliche Arme montiert werden. Diese könnten simple Winkbewegungen erlauben. Damit würde der optische Eindruck abgerundet und interessanter gestaltet.
\end{itemize}



%Je nach Art der Arbeit kann diese Kapitelüberschrift auch \glqq Ergebnisse\grqq~lauten, z.~B. bei rein messtechnischen Aufgaben.
%
%Beschreibung der Umsetzung des zuvor gewählten Vorgehens (theoretische Untersuchung, Erhebungen, Durchführung von Experimenten, Prototypenaufbau, Implementierung eines Prozesses, etc.).
%
%Verifikation anhand der zuvor erarbeiteten Anforderungen und Validierung in Bezug auf das zuvor gestellte Ziel. Diskussion der Ergebnisse. Spätestens hier auch auf die Zuverlässigkeit der gewonnenen Erkenntnisse eingehen (z.~B. anhand der Genauigkeit von Messergebnissen).